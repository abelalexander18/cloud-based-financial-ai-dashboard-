[project]
name = "financial-ai-dashboard"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "supabase",
    "python-dotenv",
]

[tool.vercel]
entrypoint = "backend.main:app"